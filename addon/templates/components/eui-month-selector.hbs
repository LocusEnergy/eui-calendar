<ol class="eui-month-selector--months">
  {{#each months as |month|}}
    {{#if hasBlock}}
      {{yield month}}
    {{else}}
      {{eui-interval
        moment=month
        interval='month'
        on-select=(action 'selectMonth')
      }}
    {{/if}}
  {{/each}}
</ol>
