<ol class="eui-month-selector--months">
  {{#each months as |month|}}
    {{eui-month
      tagName="li"
      month=month
      is-selected=(contains-month (get selection '[]') month)
    }}
  {{/each}}
</ol>
