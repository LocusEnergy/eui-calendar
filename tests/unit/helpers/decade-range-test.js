import { decadeRange } from '../../../helpers/decade-range';
import { module, test } from 'qunit';

module('Unit | Helper | decade range');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = decadeRange(42);
  assert.ok(result);
});
