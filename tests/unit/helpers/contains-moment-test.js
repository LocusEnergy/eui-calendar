import { containsMoment } from '../../../helpers/contains-moment';
import { module, test } from 'qunit';

module('Unit | Helper | contains moment');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = containsMoment(42);
  assert.ok(result);
});
