import { containsMonth } from '../../../helpers/contains-month';
import { module, test } from 'qunit';

module('Unit | Helper | contains month');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = containsMonth(42);
  assert.ok(result);
});
